<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/javascripts/jquery-3.6.4.min.js"></script>
</head>
<body bgcolor="#f0abfc">
    <% include ../partial/header %>
    <!-- <h1 class="sm:text-3xl text-2xl font-medium title-font mb-4 text-gray-900 text-center">Post List</h1>  
        
          <table>
        <thead>
            <th>Title</th>
            <th>Content</th>
            <th>Image</th>
        </thead>
        <tbody>
            <% for( var i =0 ; i < posts.length; i++){%>
            <tr>
                <td><a href="/user/postdetail/<%= posts[i]._id %>"><%= posts[i].title %></a></td>
                <td><%= posts[i].content %></td>
                <td>
                    <img src="<%= posts[i].image %>" width="100" height="50"></td>
            </tr>
            <%}%>
        </tbody>
    </table> -->
    <section class="text-gray-600 body-font">
        <h1 class="sm:text-3xl text-2xl font-medium title-font mb-4 text-gray-900 text-center">Post List</h1> 
            <div class="p-4">
              <div class="flex rounded-lg h-full bg-gray-100 p-8 flex-col w-1/2 mx-auto">
                <div class="flex-grow">
                    <table  class="table-auto">
                        <thead>
                            <th>Title</th>
                            <th>Content</th>
                            <th>Image</th>
                        </thead>
                        <tbody>
                            <% for( var i =0 ; i < posts.length; i++){%>
                            <tr>
                                <td><%= posts[i].title %></a></td>
                                <td><%= posts[i].content %></td>
                                <td>
                                    <img src="<%= posts[i].image %>" width="100" height="50"></td>
                                    <td><a href="/user/postdetail/<%= posts[i]._id %>"><span class="text-white bg-indigo-500 border-0 py-2 px-6 focus:outline-none hover:bg-indigo-600 rounded text-lg ml-5">
                                      Click Here for more Details
                                    </span></a></td>
                            </tr>
                            <%}%>
                        </tbody>
                    </table>
                  
                </div>
              </div>
            </div>
      </section>
</body>
</html>